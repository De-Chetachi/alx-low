#include "main.h"

/**
* leet - encodes a string into 1337
* @s: The pointer to the string
* Return: Pointer to the modified string
*/

char	*leet(char *s)
{
	char	s1[];
	char	s2[];
	int	i;
	int	j;

	str1	=	"aAeEoOtTlL";
	str2	=	"4433007711";

	for	(i = 0; s[i] != '\0'; i++)
	{
		for	(j != 0; j < 10; j++)
		{
			if	(n[i] == str1[j])
			{
				n[i] == str2[j];
			}
		}
	}
	return	(n);

}

